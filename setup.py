from setuptools import setup, find_packages

setup(
    name="Geometry_calculator-",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "numpy>=1.21.0",
    ],
    python_requires=">=3.8",
)
